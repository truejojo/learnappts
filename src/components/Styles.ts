import styled from "styled-components";

export const StyleWrapper = styled.div`
  background-color: #eee;
  padding: 0.5rem 0;
  
  header,
  footer {
    width: 99%;
    max-width: 1080px;
    margin: 0 auto;

  }
`;
